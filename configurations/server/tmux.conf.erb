<%= read '../common/tmux.conf' %>

# Left status bar
set -g status-left " "

# Right status bar
set -g status-interval 1
set -g status-right-length 200
set -g status-right "#[bg=colour236 fg=colour232]#[default]#(cat /tmp/tmux-info) %m/%d %a %H:%M:%S"

# Session
new -d -s server
rename-window -t server:1 weechat
send-keys -t server:1 "ssh vubuntu\n"
new-window -t server:2
new-window -t server:3 -n vubuntu
send-keys -t server:3 "ssh vubuntu\n"
new-window -t server:4
select-window -t server:1
